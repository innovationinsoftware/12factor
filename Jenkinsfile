pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'this is build'
            }
        }
        stage('release') {
            steps {
                echo 'this is release'
            }
        }
        stage('run') {
            steps {
                echo 'this is run'
            }
        }
    }
}
